{
  "name": "high-school-youth-club",
  "version": "1.0.0",
  "private": true,
  "description": "High School Youth Club — Nepali Youth Club Portal (Monorepo)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace=frontend run dev",
    "dev:backend": "npm --workspace=backend run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm --workspace=frontend run build",
    "build:backend": "npm --workspace=backend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "typecheck:frontend": "npm --workspace=frontend run typecheck",
    "typecheck:backend": "npm --workspace=backend run typecheck",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "test:backend": "npm --workspace=backend run test",
    "test:frontend": "npm --workspace=frontend run test",
    "test": "npm run test:backend && npm run test:frontend",
    "lint": "npm --workspace=frontend run lint && npm --workspace=backend run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
